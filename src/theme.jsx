import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2F3A59",
    },
    secondary: {
      main: "#738CBF",
    },
  },
});
export default theme;
